version: '3.9'

services:
  postgres:
    image: postgres
    container_name: topics_postgres_test
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: topics_db
    ports:
      - '5432:5432'
