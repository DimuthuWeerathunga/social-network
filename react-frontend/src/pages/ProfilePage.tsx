import React, { FC } from 'react';
import { Col } from 'antd';

const ProfilePage: FC = () => {
    return (
      <Col span={16} offset={4} style={{ marginTop: '4rem' }}>
        <h1>Hello there</h1>
      </Col>
    );
};

export default ProfilePage;
