version: '3.9'

services:
  postgres:
    image: postgres
    container_name: posts_postgres_test
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: users_db
    ports:
      - '5432:5432'
